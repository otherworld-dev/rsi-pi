# Client and states

`RSIClient` is what `RSIAPI` drives. Use it directly only when you need the
raw shared variables or the state machine.

## Reference

::: RSIPI.rsi_client.ClientState

::: RSIPI.rsi_client.RSIClient
