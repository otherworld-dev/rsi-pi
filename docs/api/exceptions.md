# Exceptions

Every RSIPI exception derives from `RSIError`, so `except RSIError` catches
them all. The hierarchy groups network, safety, configuration, state, data
and motion failures.

## Reference

::: RSIPI.exceptions
